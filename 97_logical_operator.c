# include <stdio.h>
int main()
{   
    int a;
    int b;
    a = 20; // Means True as int value. // TRUE
    b = 0; // Means False as Zero. // FALSE

    printf("a && b ==> %d\n",a&&b);

    printf("a || b ==> %d\n",a||b);

    printf("!b ==> %d\n",!b);

    return 0;
}
